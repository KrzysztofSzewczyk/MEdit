box:
  id: java
  ports:
    - "8080"
dev:
  steps:
    - install-packages:
        packages: ant
build:
  steps:
    - script:
        name: ant compile jar
        code: |
          ant compile jar
