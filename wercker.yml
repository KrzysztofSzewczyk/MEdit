box:
  id: openjdk
  ports:
    - "8080"
dev:
  steps:
    - script:
      name: ant compile jar
      code: |
        ant compile jar
build:
  steps:
    - script:
        name: ant compile jar
        code: |
          ant compile jar
